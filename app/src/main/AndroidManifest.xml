<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sayali.trackyourexpenses">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginScreen" />
        <activity
            android:name=".ui.DashboardScreen"
            android:label="@string/title_activity_dashboard_screen"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.ResetPassword" />
        <activity android:name=".ui.SignupScreen" />
        <activity android:name=".ui.AddCategory" />
        <activity android:name=".ui.DisplayCategory" />
        <activity android:name=".ui.EditCategory" />
        <activity android:name=".ui.AddExpense" />
        <activity android:name=".ui.DisplayCategoryExpenses" />
        <activity android:name=".ui.DisplayExpense"></activity>
    </application>

</manifest>